Artwork and placeholders no longer referenced by the site.

Kept in the repository so nothing is lost, moved out of `public/` so it is not
uploaded on every deploy. To bring one back, move it into `public/images/`.

The `placeholder.*` files are leftovers from the v0 starter template. The
`nexi-logo-white.png` at the repository root duplicated the copy that is still
live at `public/images/nexi-logo-white.png`.
